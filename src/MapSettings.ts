import MapStyles from './MapStyles'

export const containerStyle = {
	width: '100%',
	height: '100vh',
	marginTop: '-58px',
}

export const options = {
	styles: MapStyles,
	disableDefaultUI: true,
	zoomControl: true,
}
